import React from 'react';

/**
 * Web-specific implementation that does nothing
 * App open ads are not available on web
 */
export const AppStartAdManager = () => {
  // This component doesn't render anything
  return null;
};
